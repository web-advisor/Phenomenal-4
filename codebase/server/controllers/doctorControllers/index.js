const {
    createDoctor,
    updateDoctor,
    deleteDoctor,
    doctorLogin
} = require("./doctorController");

module.exports = {
    createDoctor,
    updateDoctor,
    deleteDoctor,
    doctorLogin
};